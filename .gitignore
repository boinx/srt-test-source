.DS_Store
*.log
# local config (copied from config.env.example by setup.sh)
config.env
# runtime artifacts (normally live in ~/.cache/srt-test-source, ignore any stray local copies)
mediamtx.gen.yml
*.ts
